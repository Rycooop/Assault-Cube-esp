#pragma once

extern bool bMenuShow, bEsp, bSnapLines, bEspBox, bHeadBoxes, bShadedBoxes;
extern int windowWidth, windowHeight, windowX, windowY;

struct Vec2
{
	float x, y;
};

struct Vec3
{
	float x, y, z;
};

struct Vec4
{
	float x, y, z, w;
};